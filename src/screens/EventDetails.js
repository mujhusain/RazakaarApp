import React from'react';
import {View, Text, StyleSheet, Image} from 'react-native';

export default function EventDetails({navigation}){
    return (
        <View>
            <Text style={{color:"black"}}>Event Details</Text>
        </View>
    )
}
